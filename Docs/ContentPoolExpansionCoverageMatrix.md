# Content Pool Expansion Coverage Matrix

This matrix records the current implementation slice for the content-pool expansion plan. Anomaly historical-pool playability is intentionally deferred.

| Area | Current status | Runtime entry | Focused coverage | Notes |
| --- | --- | --- | --- | --- |
| Timewarped high-impact combat minions | Implemented and third precision slice verified | `CombatEngine` timewarped start, deathrattle, avenge, rally, summon, damage, copy, and refresh branches | `MatchServiceTests`, `TimewarpedHistoricalImplementationTests` 14/14 | Bassgill/Mrrrglr-style hand pressure, Red Whelp/Ragnaros damage, Stoneshell/Guard rally, Henchman copy, Whirl-O-Tron deathrattle copy, and current-pool reward loops are covered. Precision slices now include Timewarped Deathswarmer, which immediately buffs friendly Undead during combat and still queues permanent Undead attack; Timewarped Kil'rek, whose deathrattle queues `AddRandomDemonToHand` from `CombatEngine` while avoiding duplicate player-side service handling; and Timewarped Goldrinn, whose deathrattle now immediately buffs friendly Beasts during the current combat while the existing service-layer path keeps the persistent Beast growth. Post-Goldrinn full EditMode passed: 1036 total, 1035 passed, 0 failed, 1 skipped. |
| Timewarped high-impact non-minions | Implemented | `MatchService` timewarped purchase, discover, turn-start, refresh, and direct-cast branches | `MatchServiceTests` | Big Winner, Evolving Tavern, New Recruit, Rat in a Cage, Cloning Device, Conch, Goldenizer, Thief/Master Thief, Revelation, Beanstalk, and hero-power spell cards already have focused coverage. |
| Quest/Trinket start-of-combat stacking | Implemented | `PrepareTrinketCombatStartEffects`, `PrepareQuestCombatStartEffects`, `CombatEngine` next-combat board buff | `QuestTrinketInteractionTests.StartOfCombatQuestAndTrinketBoardBuffsStackOnCombatSnapshot` | Locks the current order and confirms recruit-board state is not permanently mutated by combat-only buffs. |
| Quest/Trinket deathrattle repeat interaction | Implemented | `CombatEngine.GetDeathrattleRepeats` plus first-only trinket repeat state | `QuestTrinketInteractionTests.QuestAndTrinketDeathrattleRepeatsStackWithoutReusingFirstOnlySource` | Confirms quest repeat applies broadly while trinket first-only repeat is consumed once. |
| Quest/Trinket avenge interaction | Implemented | `ResolveQuestAvenge`, trinket avenge counters | `QuestTrinketInteractionTests.QuestAndTrinketAvengeRewardsConsumeSameDeathsIndependently` | Confirms shared death events can feed both systems without suppressing either reward. |
| Quest summon plus trinket summon modifiers | Implemented | `ResolveQuestAvenge`, `AddToken`, `ResolveFriendlySummonTriggers`, `ApplyTrinketCombatSummonModifiers` | `QuestTrinketInteractionTests.QuestSummonedMinionsReceiveTrinketAndQuestSummonModifiers` | Confirms quest-created combat summons receive both quest summon stats and trinket keyword modifiers. |
| Timewarped summon into Trinket summon reward | Implemented and full-run verified | `CombatEngine.SummonHighestHealthMinionsFromHand`, `ResolveFriendlySummonTriggers`, `QueueFriendlySummonReward`; `MatchService.ApplyTrinketFriendlySummonRewards` | `QuestTrinketInteractionTests.TimewarpedSummonTriggersTrinketSummonRewardAndStaysSideIsolated`; suite 8/8 | Locks that a Timewarped Bassgill combat summon receives Quest summon stats, queues a player `FriendlyMinionSummoned` reward, pays off Wildfeather Duster into player hand state, and leaves the opponent Timewarped Bassgill summon only in `OpponentRewards`. Post-summon-chain full EditMode passed: 1039 total, 1038 passed, 0 failed, 1 skipped. Report: `Docs/QuestTrinketTimewarpedSummonChainTestReport.md`. |
| Deathrattle repeat summons into Trinket summon counters | Implemented and full-run verified | `CombatEngine.GetDeathrattleRepeats`, Manasaber deathrattle summons, `ResolveFriendlySummonTriggers`, `QueueFriendlySummonReward`; `MatchService.ApplyTrinketFriendlySummonRewards` | `QuestTrinketInteractionTests.RepeatedDeathrattleSummonsTriggerTrinketSummonRewardsOncePerSummonAndStaySideIsolated`; `QuestTrinketInteractionTests.OpponentRepeatedDeathrattleSummonsDoNotAdvancePlayerSummonCounters`; suite 10/10 | Locks that Turbulent Tombs repeated Manasaber deathrattle creates four real Beast summon rewards, Goose Portrait and Wildfeather Duster advance exactly once per summon, and opponent Titus-repeated Manasaber summon rewards stay in `OpponentRewards` without advancing player counters. Post-repeat-summon-counter full EditMode passed: 1041 total, 1040 passed, 0 failed, 1 skipped. Report: `Docs/QuestTrinketDeathrattleRepeatSummonCounterTestReport.md`. |
| Deathrattle repeat summon counter edge cases | Implemented and full-run verified | `CombatEngine.GetDeathrattleRepeats`, `AddToken`, `RecordSummonOverflow`, `QueueFriendlySummonReward`; `MatchService.ApplyTrinketFriendlySummonRewards`, `DebugSkipToNextTurn` | `QuestTrinketInteractionTests.RepeatedDeathrattleSummonRewardsRespectBoardSpaceAndOverflow`; `DeathrattleRepeatSourcesStackBeforeSummonCounterRewards`; `NonBeastRepeatedDeathrattleSummonsDoNotAdvanceBeastSummonTrinkets`; `RepeatedDeathrattleSummonCounterRemainderPersistsAcrossCombats`; `RepeatedDeathrattleSummonRewardsDoNotDoubleCountReplayCopies`; suite 15/15 | Locks overflow-as-no-reward semantics, Turbulent Tombs plus Titus stacked repeat counts, non-Beast summon reward filtering, Wildfeather remainder persistence across combats, and replay/frame non-duplication. Post-follow-up full EditMode passed: 1046 total, 1045 passed, 0 failed, 1 skipped. Report: `Docs/QuestTrinketDeathrattleRepeatFollowupTestReport.md`. |
| Opponent reward isolation | Implemented | `CombatEngine` side rewards, `MatchService.ApplyCombatRewards` player-only application | `QuestTrinketInteractionTests.OpponentCombatRewardsDoNotApplyToPlayerRecruitState` | Opponent combat rewards are visible in the combat result but do not mutate the player's recruit state. |
| Quest/Trinket/Timewarped reward convergence | Implemented and full-run verified | `CombatEngine` friendly death, deathrattle repeat, quest avenge, side rewards; `MatchService.ApplyCombatRewards`, `ApplyTrinketFriendlyDeathRewards` | `QuestTrinketInteractionTests.CombatRewardsConvergeWithoutLeakingOpponentRewards`; suite 6/6 | Locks one same-combat scenario where Turbulent Tombs repeats Timewarped Kil'rek, Grim Freshener gains a free refresh, Lucky Tabby pays off a friendly death counter, and opponent Coldlight rewards stay in `OpponentRewards` without adding the opponent Tavern Spell to the player's hand. Post-convergence full EditMode passed: 1037 total, 1036 passed, 0 failed, 1 skipped. |
| Combat reward to next recruit ordering | Implemented and full-run verified | `MatchService.RunCombatTest`, `ApplyCombatRewards`, `DebugSkipToNextTurn`, `ApplyPostShopRefreshEffects`, `ApplyShopGrowth` | `QuestTrinketInteractionTests.CombatRewardsApplyBeforeNextRecruitRefreshAndStaySideIsolated`; suite 7/7 | Locks that player combat rewards are applied before next recruit refresh: Kil'rek adds a Demon to hand, Grim Freshener grants a free refresh, Timewarped Goldrinn buffs current shop Beasts and records future Beast shop growth, and opponent Coldlight rewards stay isolated. Post-next-turn-order full EditMode passed: 1038 total, 1037 passed, 0 failed, 1 skipped. Report: `Docs/QuestTrinketNextTurnRewardOrderTestReport.md`. |
| Darkmoon first-batch direct prizes | Implemented | `DarkmoonPrizeEngine`, `TavernSpellEngine`, `MatchService` prize helpers | `DarkmoonPrizeSystemTests` | Fresh Tab, Banana Bunch, Gacha Gift, On the House, Mageroyal Blossom, Unfurled Codex, Might of Stormwind, Rat in a Cage, The Bouncer, and Give a Dog a Bone are covered. |
| Darkmoon persistent and cross-system prizes | Implemented | Darkmoon persistent counters and shared prize execution paths | `DarkmoonPrizeSystemTests`, `MatchServiceTests` | Good Stuff, Rocking and Rolling, New Recruit, Crystallization, Evolving Tavern, Time Thief, Raise the Stakes, Gorgeous Goblet, Big Brann Play, Open Bar, Big Winner, and related ordering-sensitive paths are covered. |

## Deferred

| Area | Reason |
| --- | --- |
| Anomaly historical-pool playability | Out of scope for this implementation slice. |
| Full official data parity for every Timewarped card | Current target is high-impact playable precision, not complete official parity. |
| Additional darkmoon edge ordering beyond existing P0/P1/P2 tests | Current Darkmoon coverage already exceeds the first-batch target; future work should be driven by failing scenarios or design needs. |
